program HelloWorld;
program Hello World2;
(*Alfa*)
var
   alta : int;
    BaT : int;
    (*Alfa*)
    connecr : boolean;
    noconnecr : boolean;

(*Alfa*)

begin
    alta := 5+5+5+1;
    alta:=((66+9)+11);
    alta := 5+5+5+2;
    if ( connecr ) then else ; 
    noconnecr := false;
    BaT := ln(1);
    alta:=ln (9);
    (*Alfa*)
    if ( noconnecr ) then else ;
    readln(noconnecr); 
    if ( noconnecr ) then else ; 
    readln(alta,BaT);
    writeln('HELLO');
    writeln(alta);
    writeln('HELLO' + alta);
end.


